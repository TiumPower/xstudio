<%= yield %>

---
<%= @workspace.name %> — https://<%= ENV.fetch("APP_HOST", "xstudio.czin.net") %>
Tắt loại thông báo này trong Hồ sơ cá nhân: https://<%= ENV.fetch("APP_HOST", "xstudio.czin.net") %>/ho-so
